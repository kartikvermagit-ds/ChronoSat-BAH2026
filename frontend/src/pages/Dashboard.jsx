export default function DashboardPage() {
  return (
    <section className="page-copy">
      <h2>Dashboard</h2>
      <p>
        This view is reserved for richer map overlays, frame scrubbing, and
        side-by-side comparisons.
      </p>
    </section>
  );
}
